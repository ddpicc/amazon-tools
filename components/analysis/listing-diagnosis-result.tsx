export function ListingDiagnosisResult({ result }: { result: Record<string, unknown> }) { const findings = Array.isArray(result.findings) ? result.findings as Array<{ label?: string; status?: string; advice?: string }> : []; return <section className="mx-auto max-w-4xl rounded-2xl border border-[var(--md-outline-variant)] bg-[var(--md-surface-container)] p-6"><h2 className="font-headline text-xl font-semibold">Listing 健康诊断</h2><p className="mt-1 font-mono text-sm text-[var(--md-on-surface-variant)]">{String(result.asin ?? "")}</p><div className="mt-5 space-y-3">{findings.map(item => <div key={item.label} className="rounded-lg border border-[var(--md-outline-variant)] p-4"><p className="font-label font-semibold">{item.label} · <span className={item.status === "PASS" ? "text-emerald-400" : "text-amber-300"}>{item.status === "PASS" ? "正常" : "需处理"}</span></p><p className="mt-1 font-label text-sm text-[var(--md-on-surface-variant)]">{item.advice}</p></div>)}</div><p className="mt-5 font-label text-xs text-[var(--md-on-surface-variant)]">限制：{String(result.limitation ?? "未知")}</p></section>; }
